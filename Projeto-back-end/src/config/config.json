{
  "development": {
    "username": "${DB_USER}",
    "password": "${DB_PASS}",
    "database": "${DB_NAME}",
    "host": "${DB_HOST}",
    "dialect": "mysql"
  },
  "_deprecated": "Este arquivo foi DESCONTINUADO. Use variáveis de ambiente em database/index.ts em vez disso. Ver SECURITY.md para mais detalhes."
}
